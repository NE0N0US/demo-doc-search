services:
  app:
    container_name: csm-demo
    ports:
      - 5173:5173
    build:
      context: .
      dockerfile_inline: |
        FROM node:22-alpine
        WORKDIR /app
        COPY . .
        RUN npm i
        EXPOSE 5173
        CMD npm start
